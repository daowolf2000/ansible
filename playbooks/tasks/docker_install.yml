---
# Установка Docker-CE
# os  : Deb*
# req : Internet, x_package_req

- name: Initialization variables
  ansible.builtin.set_fact:
    x_username: "{{ x_username | default(ansible_user_id) }}"
    x_package_req:
      - ca-certificates
      - curl
      - gnupg
    x_package:       
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-compose
      - docker-compose-plugin



- name: Defiane DEB arch
  ansible.builtin.command: |
    dpkg --print-architecture
  changed_when: false
  register: x_deb_arch

- name: Define os codename
  ansible.builtin.shell: |
    source /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}"
  args:
    executable: /bin/bash  
  changed_when: false
  register: x_os_codename

- name: Ensure pre-requisites are installed
  ansible.builtin.apt:
    name: "{{ x_package_req }}" 
    state: present
  become: yes

- name: Create directory for Docker GPG keyring
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    mode: '0755'
  become: yes

- name: Download and install Docker's official GPG key
  ansible.builtin.get_url:
    url: https://download.docker.com/linux/debian/gpg
    dest: /etc/apt/keyrings/docker.asc
    mode: '0644'
  become: yes

# Добавляем репозиторий Docker в источники APT
- name: Add Docker repository to Apt sources
  ansible.builtin.blockinfile:
    path: /etc/apt/sources.list.d/docker.list
    create: true
    block: |
      deb [arch={{ x_deb_arch.stdout }} signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/{{ distro_map[ansible_distribution] }} {{ x_os_codename.stdout }} stable
    marker: "# {mark} Ansible managed block for Docker repo"
    mode: '0644'
  vars:
    distro_map:
      Linux Mint: ubuntu
      Ubuntu:     ubuntu
      Debian:     debian
  register: x_source_update
  become: yes

- name: Update apt sources
  ansible.builtin.apt:
    update_cache: yes
  when: x_source_update is changed
  become: yes

- name: Install Docker Engine and related packages
  ansible.builtin.apt:
    name: "{{ x_package }}"
    state: present
  become: yes

- name: Enable Docker to start at boot
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started
  become: yes

- name: Add user to the docker group
  ansible.builtin.user:
    name: "{{ x_username }}"
    groups: docker
    append: yes
  become: yes

- name: Test Docker installation
  ansible.builtin.command: docker run hello-world
  register: x_res
  changed_when: false
  failed_when: x_res.rc != 0
