# Определение каталогов
secret_vault: "{{ lookup('env', 'ANSIBLE_DIR') + '/.secret-vault' }}"

# Определение инфраструктуры
ansible_ssh_private_key_file: "{{ lookup('env', 'HOME') + '/.ssh/id_ed25519' }}"

